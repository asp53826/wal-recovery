name: ci

on:
  push:
  pull_request:

jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - name: Build and run unit tests
        run: make test
      - name: Run short crash campaign
        run: make crash-test CRASH_ROUNDS=50

